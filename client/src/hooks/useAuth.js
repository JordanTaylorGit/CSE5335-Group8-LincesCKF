/**
 * useAuth.js — Convenience hook for auth state
 * Student 1 | Authentication & User Management
 *
 * Re-exports the useAuth hook from AuthContext so components can import
 * from either '@hooks/useAuth' or '@context/AuthContext' — both work.
 */
export { useAuth, AuthProvider } from '../context/AuthContext';
